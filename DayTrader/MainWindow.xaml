<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DayTrader"
        mc:Ignorable="d" 
        Title="Daytrade scanner v1.3 (c) 2018 Erwin Beckers"  Width="900" Height="480"
        x:Class="DayTrader.MainWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Background="#cccccc">
            <Menu Margin="5,5,5,5">
                <MenuItem Header="File">
                    <MenuItem Header="Settings" Name="menuItemSettings"/>
                    <MenuItem Header="Quit"  Name="menuItemQuit"/>
                </MenuItem>
            </Menu>
        </StackPanel>

        <Grid Grid.Row="1" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Button Name="btnStart"  Content="{Binding StartButton}" Grid.Column="0" />
            <Button Name="btnDonate" Content="Donate"  Grid.Column="2" />
        </Grid>

        <ListBox Grid.Row="2" Margin="5,26,0,0" ItemsSource="{Binding Signals}" HorizontalContentAlignment="Stretch">
            <ListBox.Template>
                <ControlTemplate>
                    <DockPanel LastChildFill="True">
                        <Grid DockPanel.Dock="Top" Height="30" Background="LightGray">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"></ColumnDefinition>
                                <ColumnDefinition Width="60"></ColumnDefinition>
                                <ColumnDefinition Width="60"></ColumnDefinition>
                                <ColumnDefinition Width="80"></ColumnDefinition>
                                <ColumnDefinition Width="90"></ColumnDefinition>
                                <ColumnDefinition Width="130"></ColumnDefinition>
                                <ColumnDefinition Width="90"></ColumnDefinition>
                                <ColumnDefinition Width="130"></ColumnDefinition>
                                <ColumnDefinition Width="80"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0">Date</Label>
                            <Label Grid.Column="1">Symbol</Label>
                            <Label Grid.Column="2">Trade</Label>
                            <Label Grid.Column="3">Timeframe</Label>
                            <Label Grid.Column="4">QuoteCurrency</Label>
                            <Label Grid.Column="5">QuoteCurrencyVolume</Label>
                            <Label Grid.Column="6">BaseCurrency</Label>
                            <Label Grid.Column="7">BaseCurrencyVolume</Label>
                            <Label Grid.Column="8">Action</Label>
                        </Grid>
                        <ItemsPresenter></ItemsPresenter>
                    </DockPanel>
                </ControlTemplate>
            </ListBox.Template>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"></ColumnDefinition>
                            <ColumnDefinition Width="60"></ColumnDefinition>
                            <ColumnDefinition Width="60"></ColumnDefinition>
                            <ColumnDefinition Width="80"></ColumnDefinition>
                            <ColumnDefinition Width="90"></ColumnDefinition>
                            <ColumnDefinition Width="130"></ColumnDefinition>
                            <ColumnDefinition Width="90"></ColumnDefinition>
                            <ColumnDefinition Width="130"></ColumnDefinition>
                            <ColumnDefinition Width="80"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Date}" Grid.Column="0" />
                        <TextBlock Text="{Binding Symbol}" Grid.Column="1" />
                        <TextBlock Text="{Binding Trade}" Grid.Column="2"/>
                        <TextBlock Text="{Binding TimeFrame}" Grid.Column="3" />
                        <TextBlock Text="{Binding Volume.QuoteCurrency}" Grid.Column="4"/>
                        <TextBlock Text="{Binding Volume.QuoteCurrencyVolume}" Grid.Column="5"/>
                        <TextBlock Text="{Binding Volume.BaseCurrency}" Grid.Column="6"/>
                        <TextBlock Text="{Binding Volume.BaseCurrencyVolume}" Grid.Column="7"/>

                        <!-- 
                            		public string QuoteCurrency { get; set; }
		public decimal QuoteCurrencyVolume { get; set; }
		public string BaseCurrency { get; set; }
		public decimal BaseCurrencyVolume { get; set; }
                            -->
                        <Button Content="Open" Grid.Column="8" Click="SignalButton_Clicked"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <!--
        <ListBox Grid.Row="2" Margin="5,0,0,0" ItemsSource="{Binding Signals}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Date}" Width="100" />
                        <TextBlock Text="{Binding Symbol}" Width="100" />
                        <TextBlock Text="{Binding Trade}" Width="100" />
                        <TextBlock Text="{Binding Timeframe}" Width="100" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Background="#cccccc">
            <TextBlock VerticalAlignment="Center" Margin="5,5,5,5" Text="{Binding StatusText}">
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>